# Application
PORT=8080
NODE_ENV=development

# Database
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=team_hub

# Optional: comma-separated allowed origins (default: reflect browser origin)
# CORS_ORIGIN=http://localhost:3000
